from setuptools import setup, find_packages

setup(
    name='torchblocks',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Sebas95',
    description='Reusable code blocks for Pytorch training and evaluation',
)
